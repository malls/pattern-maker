{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default capability for the main window; the app needs nothing from the host",
  "windows": ["main"],
  "permissions": ["core:default"]
}
